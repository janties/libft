/*#include <stdio.h>

void	ft_bzero(void *s, size_t n)
{
	ft_memset(s, 0, n);
}

void *ft_calloc(size_t nmemb, size_t size)
{
	char *ptr = malloc(size * nmemb);
	bzero(ptr, size);
	return ptr;
}

int main()
{
	ft_calloc(3, 5);
}*/
